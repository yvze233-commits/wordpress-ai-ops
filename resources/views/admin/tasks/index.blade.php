@extends('admin.layout', ['title' => '任务模式'])
@section('content')
<div class="page-heading"><div><div class="eyebrow">自动运营任务</div><h1>任务模式</h1><p class="subtitle">配置每日数量、写作方式、审核流程、选题来源和 WordPress 写入方式。</p></div><span class="badge blue">用户可配置</span></div>
@if(session('status'))<div class="notice">{{ session('status') }}</div>@endif
@if($errors->any())<div class="notice">{{ $errors->first() }}</div>@endif
<section class="panel"><div class="panel-header"><div><h2>新建任务</h2><p>任务保存后默认暂停，确认配置后再启动。</p></div></div><div class="panel-body"><form method="post" action="{{ route('admin.tasks.store') }}" class="form-grid">@csrf
<label>任务名称<input name="name" required placeholder="每日品牌内容"></label><label>每日文章数量<input name="daily_target" type="number" min="1" max="20" value="4" required><small>建议设置为 3-5 篇。</small></label>
<label>写作方式<select name="writing_skill_slug" required>@foreach($writingSkills as $skill)<option value="{{ $skill->slug }}">{{ $skill->name }}</option>@endforeach</select></label><label>审核流程<select name="review_skill_slug" required><option value="geoflow_two_pass">GEOFlow 二段审核</option>@foreach($reviewSkills as $skill)@if(!in_array($skill->slug, ['geoflow_review_facts','geoflow_review_publish'], true))<option value="{{ $skill->slug }}">{{ $skill->name }}</option>@endif @endforeach</select></label>
<label>审核通过分数<input name="review_pass_threshold" type="number" min="1" max="100" value="70" required></label><label>计划运行时间<input name="schedule_time" type="time" value="02:00" required></label>
<label>选题来源<select name="topic_source_mode"><option value="all">热点和标题库</option><option value="hot">仅热点</option><option value="titles">仅标题库</option></select></label><label>写入方式<select name="publish_status"><option value="draft">WordPress 草稿</option><option value="pending">WordPress 待审核</option><option value="publish">直接发布</option></select></label>
<label>热点关键词<textarea name="topic_keywords" rows="2" placeholder="教育, 人工智能, 品牌相关词"></textarea><small>多个关键词用逗号分隔，留空使用系统默认热点。</small></label><label>热点时间范围<select name="topic_window_hours"><option value="24">最近 24 小时</option><option value="72" selected>最近 72 小时</option><option value="168">最近 7 天</option></select></label><label>最低来源可信度<input name="min_source_trust" type="number" min="0" max="100" value="50" required></label><label>写入站点<select name="wordpress_connection_id"><option value="">自动选择可用站点</option>@foreach($wordpressConnections as $connection)<option value="{{ $connection->id }}">{{ $connection->name }}</option>@endforeach</select></label>
<label class="check-field"><input type="checkbox" name="require_images" value="1" checked><span><strong>必须有配图</strong><small>没有合适图片时进入人工复核。</small></span></label><label class="check-field"><input type="checkbox" name="require_source_links" value="1" checked><span><strong>必须有来源链接</strong><small>关键结论没有来源时不自动通过。</small></span></label>
<div class="settings-actions" style="grid-column:1/-1"><button class="button" type="submit">保存任务</button></div></form></div></section>
<section class="panel" style="margin-top:19px"><div class="panel-header"><div><h2>任务列表</h2><p>启动后按计划生成当天内容；暂停只停止新批次。</p></div></div><div class="panel-body table-wrap"><table><thead><tr><th>任务</th><th>每日数量</th><th>写作方式</th><th>审核方式</th><th>写入方式</th><th>计划时间</th><th>状态</th><th>操作</th></tr></thead><tbody>
@forelse($tasks as $task)@php($topicMode = ['all' => '热点和标题库', 'both' => '热点和标题库', 'hot' => '仅热点', 'hot_only' => '仅热点', 'titles' => '仅标题库', 'title_only' => '仅标题库', 'title_library' => '仅标题库', '热点和标题库' => '热点和标题库', '仅热点' => '仅热点', '仅标题库' => '仅标题库'][$task->settings['topic_source_mode'] ?? 'all'] ?? '热点和标题库')<tr><td><strong>{{ $task->name }}</strong><br><small>审核 {{ $task->review_pass_threshold }} 分 · {{ $topicMode }}</small></td><td>{{ $task->daily_target }} 篇</td><td>{{ $writingSkills->firstWhere('slug', $task->writing_skill_slug)?->name ?? $task->writing_skill_slug }}</td><td>{{ $task->review_skill_slug === 'geoflow_two_pass' ? 'GEOFlow 二段审核' : ($reviewSkills->firstWhere('slug', $task->review_skill_slug)?->name ?? $task->review_skill_slug) }}</td><td>{{ ['draft' => '草稿', 'pending' => '待审核', 'publish' => '直接发布'][$task->publish_status] ?? '草稿' }}</td><td>{{ $task->schedule_time }}</td><td><span class="badge {{ $task->status === 'running' ? 'teal' : 'gray' }}">{{ $task->status === 'running' ? '运行中' : '已暂停' }}</span></td><td><div class="actions">@if($task->status === 'running')<form method="post" action="{{ route('admin.tasks.pause', $task) }}">@csrf<button class="button warn" type="submit">暂停任务</button></form><form method="post" action="{{ route('admin.tasks.run', $task) }}">@csrf<button class="button" type="submit">运行今日任务</button></form>@else<form method="post" action="{{ route('admin.tasks.start', $task) }}">@csrf<button class="button" type="submit">启动任务</button></form>@endif
<details><summary class="button ghost">编辑配置</summary><form method="post" action="{{ route('admin.tasks.update', $task) }}" style="margin-top:8px" class="form-grid">@csrf<label>任务名称<input name="name" value="{{ $task->name }}" required></label><label>每日数量<input name="daily_target" type="number" min="1" max="20" value="{{ $task->daily_target }}" required></label><label>写作方式<select name="writing_skill_slug">@foreach($writingSkills as $skill)<option value="{{ $skill->slug }}" @selected($task->writing_skill_slug === $skill->slug)>{{ $skill->name }}</option>@endforeach</select></label><label>审核流程<select name="review_skill_slug"><option value="geoflow_two_pass" @selected($task->review_skill_slug === 'geoflow_two_pass')>GEOFlow 二段审核</option>@foreach($reviewSkills as $skill)@if(!in_array($skill->slug, ['geoflow_review_facts','geoflow_review_publish'], true))<option value="{{ $skill->slug }}" @selected($task->review_skill_slug === $skill->slug)>{{ $skill->name }}</option>@endif @endforeach</select></label><label>审核分数<input name="review_pass_threshold" type="number" min="1" max="100" value="{{ $task->review_pass_threshold }}"></label><label>运行时间<input name="schedule_time" type="time" value="{{ $task->schedule_time }}"></label><label>选题来源<select name="topic_source_mode"><option value="all" @selected(in_array($task->settings['topic_source_mode'] ?? 'both', ['all','both','热点和标题库'], true))>热点和标题库</option><option value="hot" @selected(in_array($task->settings['topic_source_mode'] ?? '', ['hot','hot_only','仅热点'], true))>仅热点</option><option value="titles" @selected(in_array($task->settings['topic_source_mode'] ?? '', ['titles','title_only','仅标题库'], true))>仅标题库</option></select></label><label>写入方式<select name="publish_status"><option value="draft" @selected($task->publish_status === 'draft')>WordPress 草稿</option><option value="pending" @selected($task->publish_status === 'pending')>WordPress 待审核</option><option value="publish" @selected($task->publish_status === 'publish')>直接发布</option></select></label><label>热点关键词<textarea name="topic_keywords" rows="2">{{ $task->topic_keywords }}</textarea></label><label>热点时间范围<select name="topic_window_hours"><option value="24" @selected($task->topic_window_hours === 24)>最近 24 小时</option><option value="72" @selected($task->topic_window_hours === 72)>最近 72 小时</option><option value="168" @selected($task->topic_window_hours === 168)>最近 7 天</option></select></label><label>最低来源可信度<input name="min_source_trust" type="number" min="0" max="100" value="{{ $task->min_source_trust }}"></label><label>写入站点<select name="wordpress_connection_id"><option value="">自动选择可用站点</option>@foreach($wordpressConnections as $connection)<option value="{{ $connection->id }}" @selected($task->wordpress_connection_id === $connection->id)>{{ $connection->name }}</option>@endforeach</select></label><label class="check-field"><input type="checkbox" name="require_images" value="1" @checked($task->settings['require_images'] ?? true)><span>必须有配图</span></label><label class="check-field"><input type="checkbox" name="require_source_links" value="1" @checked($task->settings['require_source_links'] ?? true)><span>必须有来源链接</span></label><div class="settings-actions" style="grid-column:1/-1"><button class="button" type="submit">保存配置</button></div></form></details></div></td></tr>@empty<tr><td colspan="8" class="empty">暂无任务，请先创建一个每日发文任务。</td></tr>@endforelse
</tbody></table></div></section>
@endsection
